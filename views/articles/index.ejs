<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <title>Blog</title>
</head>
<body>
    <div class="d-flex flex-column mt-">
        <h1 class="p-3">Blog Articles</h1>
        <a class="btn btn-primary m-3" href="/articles/new">New Articles</a>

        <% articles.forEach(article => { %>
            <div class="container p-3 my-3 border">
                <div class="">
                    <h2><%= article.title %></h2>
                </div>
                <div class="">
                    <%= article.createdAt.toLocaleDateString() %>
                </div>
                <div class="">
                    <%= article.description %>
                </div>
                <div class="d-flex flex-row">
                    <div class="p-2">
                        <a class="btn btn-success" href="articles/<%= article.slug %>">Read More</a>
                    </div>
                    <div class="p-2">
                        <a class="btn btn-warning" href="articles/<%= article.id %>">Edit</a>
                    </div>
                    <form class="p-2" action="/articles/<%= article.id %>?_method=DELETE" method="POST">
                        <button class="btn btn-danger" type="submit">Delete</button>
                    </form>
                </div>
            </div>
        <% }) %>
    </div>
</body>
</html>